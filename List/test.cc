#include "Queue.hh"
#include "Stack.hh"
#include <iostream>

using namespace std;

int main() {
  /*  List<int> v;
  cout << "v size: " << v.size() << endl;

  v.push_back(10);
  v.push_back(3);
  v.push_back(51);
  v.push_back(4);
  v.push_back(5);
  v.push_back(71);

  v.print();
  cout << endl << "Queue: " << endl;
  Queue<int> q;

  q.push(13);
  q.push(14);
  q.push(15);
  q.push(16);
  q.push(17);
  q.push(18);
  q.print();
  cout << "q size: " << q.size() << endl;

  q.pop();
  q.print();
  cout << endl;
  cout << "front: " << q.front() << endl;
  cout << "back: " << q.back() << endl;*/ // here

  // Prefixpostfix class

  return 0;
}
